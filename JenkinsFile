pipline {
    agent any

    stages {

        stage('Checkout') {

            steps {
                echo 'checkout from repo'
            }
        }

        stage('Test') {

            steps {
                echo 'test'
            }
        }

        stage('Build') {
            steps {
                echo 'build'
            }
        }
    }
}